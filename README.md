# BROWSER-EXTENSION-MANAGER
A fully responsive browser extensions manager UI built with HTML, CSS, and vanilla JavaScript, featuring dynamic rendering from JSON, state persistence via localStorage, and theme switching using CSS variables.
